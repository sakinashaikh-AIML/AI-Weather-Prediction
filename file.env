OPENWEATHER_API_KEY="fc37682183690a8f08872139556bc720"
#pip install flask requests numpy pandas scikit-learn joblib python-dotenv

# fix_env.py
with open('.env', 'rb') as f:
    content = f.read()

# Check if it's UTF-16
if content.startswith(b'\xff\xfe'):  # UTF-16 LE BOM
    text = content.decode('utf-16')
elif content.startswith(b'\xfe\xff'):  # UTF-16 BE BOM
    text = content.decode('utf-16')
else:
    # Try UTF-8
    text = content.decode('utf-8')

# Save as UTF-8 without BOM
with open('.env', 'w', encoding='utf-8') as f:
    f.write(text)

print("✅ .env file fixed!")